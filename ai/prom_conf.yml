global:
  scrape_interval: 2s # Set the scrape interval to every 10 seconds. Default is every 1 minute.
  evaluation_interval: 2s # Evaluate rules every 10 seconds. The default is every 1 minute.
scrape_configs:
- job_name: 'ray'
  file_sd_configs:
  - files:
    - '/tmp/ray/prom_metrics_service_discovery.json'
    refresh_interval: 10s
    # [{"labels": {"job": "ray"}, "targets": ["127.0.0.1:58549", "127.0.0.1:44217", "127.0.0.1:44227"]}] 